<Window x:Class="SS14_MIDI_IDE.SuccessDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Success"
        Icon="pack://application:,,,/mothroach.ico"
        Height="150" Width="350"
        Background="#1e1e1e"
        Foreground="White"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Border CornerRadius="5" Background="#2d2d30" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar Header -->
            <DockPanel Grid.Row="0" Height="30" Background="#3e3e42" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="Success" Foreground="White" VerticalAlignment="Center" Margin="10,0" FontWeight="Bold"/>
                
                <!-- Close Button -->
                <Button Width="30" Height="30" HorizontalAlignment="Right" Margin="0,0,5,0" Click="CloseButton_Click" Foreground="White">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Name="border" Background="Transparent" CornerRadius="3">
                                <Path Name="icon" Data="M 0,0 L 10,10 M 10,0 L 0,10" Stroke="#999" StrokeThickness="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#e81123"/>
                                    <Setter TargetName="icon" Property="Stroke" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </DockPanel>
            
            <!-- Window Content Goes Here -->
            <Grid Grid.Row="1" Margin="15">
                <TextBlock x:Name="MessageText" Text="The operation was successful." Foreground="White" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
            
            <Button Grid.Row="2" Content="OK" Width="80" Height="25" HorizontalAlignment="Center" Background="#007acc" Foreground="White" BorderBrush="Transparent" Click="CloseButton_Click"/>
        </Grid>
    </Border>
</Window>
