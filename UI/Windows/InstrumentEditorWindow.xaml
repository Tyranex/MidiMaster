<Window x:Class="SS14_MIDI_IDE.InstrumentEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Instrument Editor" Height="600" Width="800"
        Background="#1e1e1e" Foreground="#d4d4d4"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="None"
        UseLayoutRounding="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="28" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="WindowCloseBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#aaaaaa"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets, Segoe UI Symbol"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#e81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#f1707a"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="InstrumentItemTemplate">
            <StackPanel Orientation="Horizontal" Margin="2">
                <TextBlock Text="{Binding Id, StringFormat='{}{0:D2}'}" Foreground="#888" Width="30"/>
                <TextBlock Text="{Binding Name}" Foreground="#d4d4d4"/>
            </StackPanel>
        </DataTemplate>
        
        <!-- Style overrides to match dark theme -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#aaa"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0 0 0 2"/>
                    <Setter Property="BorderBrush" Value="#007acc"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Dark Title Bar -->
        <Grid Grid.Row="0" Background="#1e1e1e">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <TextBlock Text="Instrument Editor" Foreground="#cccccc" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <Border Grid.Column="1" Background="Transparent" WindowChrome.IsHitTestVisibleInChrome="False"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Style="{StaticResource WindowCloseBtnStyle}" Content="&#xE8BB;" ToolTip="Close" Click="WindowClose_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Panel: Instrument List -->
        <Border Grid.Column="0" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Instruments" FontWeight="Bold" Margin="10,10,10,5"/>
                
                <ListBox x:Name="InstrumentListBox" Grid.Row="1" Background="Transparent" Foreground="#d4d4d4" 
                         BorderThickness="0" SelectionChanged="InstrumentListBox_SelectionChanged"
                         ItemTemplate="{DynamicResource InstrumentItemTemplate}"/>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                    <Button Content="New Inst" Width="80" Margin="5" Padding="5,2" Background="#333" Foreground="White" BorderBrush="#555" Click="NewInst_Click"/>
                    <Button Content="Delete" Width="80" Margin="5" Padding="5,2" Background="#333" Foreground="White" BorderBrush="#555" Click="DeleteInst_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" Background="Transparent"/>
        
        <!-- Right Panel: Editor -->
        <Border Grid.Column="2" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="3">
            <Grid x:Name="EditorPanel" IsEnabled="False" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Name:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="InstNameBox" Width="200" Background="#333" Foreground="White" BorderBrush="#555" Padding="3" TextChanged="InstNameBox_TextChanged"/>
                    
                    <TextBlock Text="Chip:" VerticalAlignment="Center" Margin="20,0,10,0"/>
                    <ComboBox x:Name="ChipComboBox" Width="100" Background="#333" Foreground="White" BorderBrush="#555" SelectionChanged="ChipComboBox_SelectionChanged">
                        <ComboBoxItem Content="2A03"/>
                        <ComboBoxItem Content="VRC6"/>
                    </ComboBox>
                </StackPanel>
                
                <!-- Macro Tabs -->
                <TabControl x:Name="MacroTabControl" Grid.Row="1" Background="Transparent" BorderThickness="0 0 0 1" BorderBrush="#3e3e42" SelectionChanged="MacroTabControl_SelectionChanged">
                    <TabItem Header="Volume" Tag="Volume"/>
                    <TabItem Header="Arpeggio" Tag="Arpeggio"/>
                    <TabItem Header="Pitch" Tag="Pitch"/>
                    <TabItem Header="Hi-Pitch" Tag="HiPitch"/>
                    <TabItem Header="Duty" Tag="Duty"/>
                </TabControl>
                
                <!-- Macro Graph Canvas -->
                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Length:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="SeqLengthBox" Width="40" Text="0" Background="#333" Foreground="White" BorderBrush="#555" TextAlignment="Center" TextChanged="SeqLengthBox_TextChanged"/>
                        
                        <TextBlock Text="Loop:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                        <TextBox x:Name="SeqLoopBox" Width="40" Text="-1" Background="#333" Foreground="White" BorderBrush="#555" TextAlignment="Center" TextChanged="SeqLoopBox_TextChanged"/>
                        
                        <TextBlock Text="Release:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                        <TextBox x:Name="SeqReleaseBox" Width="40" Text="-1" Background="#333" Foreground="White" BorderBrush="#555" TextAlignment="Center" TextChanged="SeqReleaseBox_TextChanged"/>
                    </StackPanel>
                    
                    <Border Grid.Row="1" Background="#1e1e1e" BorderBrush="#555" BorderThickness="1">
                        <!-- Custom drawing canvas for graph -->
                        <Canvas x:Name="GraphCanvas" Background="Transparent" 
                                MouseLeftButtonDown="GraphCanvas_MouseLeftButtonDown"
                                MouseMove="GraphCanvas_MouseMove"
                                MouseLeftButtonUp="GraphCanvas_MouseLeftButtonUp"
                                SizeChanged="GraphCanvas_SizeChanged"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
    </Grid>
</Window>
